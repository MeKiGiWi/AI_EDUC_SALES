# Motion Rules

- Prefer fade, blur, and slight upward reveal.
- Use durations around `500ms` to `900ms`.
- Decorative drift should be slow and barely noticeable.
- Avoid bounce, overshoot, and playful physics.
